[Unit]
Description=Coach Go Server
After=network.target

[Service]
ExecStart=/opt/coach/coach
Restart=always
User=root
Group=root
Environment=PATH=/usr/bin:/usr/local/bin
WorkingDirectory=/opt/coach

[Install]
WantedBy=multi-user.target
