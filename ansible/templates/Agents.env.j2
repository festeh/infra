{# Templated to /opt/my-agents/.env. Vars come from secrets.yml. #}
{# DO NOT add comments inline with values — systemd EnvironmentFile is strict. #}
AGENTS_HOST=127.0.0.1
AGENTS_PORT=8001

CLIPROXYAPI_URL={{ cliproxyapi_url }}
CLIPROXYAPI_API_KEY={{ cliproxyapi_api_key }}

LANGSMITH_API_KEY={{ langsmith_api_key }}
LANGSMITH_TRACING=true

DIMAIST_URL={{ dimaist_url }}
COACH_URL={{ coach_url | default('http://127.0.0.1:8080') }}
COACH_TZ={{ coach_tz | default('Europe/Madrid') }}
